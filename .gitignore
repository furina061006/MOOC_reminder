# Dependencies
node_modules/

# 运行时输出（进程自己吐的日志），不是项目文档。
# 这里**故意不写** `logs/` 这种「忽略任何叫 logs 的目录」的规则：本项目要共享的开发日志
# 就在 `.dsh/logs/`，那条规则曾把它整个吞掉，害得一批日志没进仓库（2026-09-21）。
# 只按文件类型挡，文档目录就永远不会被误伤。
*.log
npm-debug.log*

# .dsh/ 是项目文档目录：只跟踪 logs/（更新日志 + 开发日志，就是要共享的部分）和
# agents/（分主题知识库），其余任何东西（各工具的本机 settings 等）一律不进仓库。
.dsh/*
!.dsh/logs/
!.dsh/logs/**
!.dsh/agents/
!.dsh/agents/**

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build output
dist/
build/
mooc-reminder*.zip

# Environment
.env
.env.local

# AI 工具的本机目录 —— 仓库不再使用 .claude/（2026-09-21 已把里面有用的东西折进
# AGENTS.md / .dsh/agents/，目录整个删掉）。这条规则只是安全带：以后谁再跑 Claude Code
# 或别的兼容工具，它自动生成的 settings 不会被顺手提交。
.claude/

# reference projects
reference_projects/

# zcode —— AI 规划工具的本地目录。这条规则以前写成 `zcode/`（少了点），
# 挡不住真实目录，于是 `.zcode/plans/*.md` 曾被提交进仓库（2026-09-21 已 git rm）。
.zcode/

# Local diagnostic dumps — raw DOM snapshots and API captures contain the user's
# own course/account data (userId, course ids, teacher content) and this repo is
# public. See tools/diagnostics/README.md.
element.txt
mooc-dto-report.json
dto*.json
*-report.json
*.har
# Windows「另存为」留下的 Mark-of-the-Web 备用数据流，会在 WSL 里落成一个独立文件
*:Zone.Identifier
